/**
 * This file is referenced by the imports field in `package.json` for `#arrayObjectContext`.
 *
 * It allows import types such as the following to resolve:
 * ```js
 * /** @import { ItemContext, ItemEntryStore } from '#arrayObjectContext'; *\/
 * ```
 * 
 * This is just one way to type via JSDoc. Also see the side slide layer demo on how to use a separate TypeScript file
 * to type your Svelte components with JSDoc: `src/view/standard-components/side-slide-layer/types.ts`.
 */

export * from './ItemArrayObjectStore.js';
export * from './ItemContext.js';
